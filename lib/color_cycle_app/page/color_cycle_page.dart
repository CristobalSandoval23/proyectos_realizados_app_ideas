import 'package:flutter/material.dart';

class ColorCyclePage extends StatelessWidget {
  const ColorCyclePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}